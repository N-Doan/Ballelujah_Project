using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestNomadAbilities : MonoBehaviour
{/*
    [SerializeField]
    FireNomad fireScript;
    [SerializeField]
    WaterNomad waterScript;
    [SerializeField]
    EarthNomad earthScript;
    [SerializeField]
    AirNomad airScript;
    // Update is called once per frame
    void Update()
    {
        
        if (Input.GetKeyDown("1"))
        {
            fireScript.activateAbility(true);
        }
        else if (Input.GetKeyDown("2"))
        {
            waterScript.activateAbility(false);
        }
        else if (Input.GetKeyDown("3"))
        {
            earthScript.activateAbility(false);
        }
        else if (Input.GetKeyDown("4"))
        {
            airScript.activateAbility(true);
        }
    }*/
}
